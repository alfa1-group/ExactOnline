// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Crm.ReopenQuotation;

public partial class ReopenQuotationRequestBuilder
{
    public async Task<CRMReopenQuotation_Response?> GetAllAsync(Action<RequestConfiguration<ReopenQuotationRequestBuilder.ReopenQuotationRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.CRMReopenQuotationResults is null)
        {
            return response;
        }

        var allItems = new List<CRMReopenQuotation>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.CRMReopenQuotationResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.CRMReopenQuotationResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ReopenQuotationRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new CRMReopenQuotation_Response
        {
            D = new CRMReopenQuotation_Response.CRMReopenQuotation_Response_d
            {
                CRMReopenQuotation = allItems
            }
        };

        return finalResponse;
    }
}
