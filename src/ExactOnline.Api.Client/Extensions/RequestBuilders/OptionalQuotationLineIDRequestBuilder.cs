// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Crm.OptionalQuotationLineID;

public partial class OptionalQuotationLineIDRequestBuilder
{
    public async Task<CRMOptionalQuotationLineID_Response?> GetAllAsync(Action<RequestConfiguration<OptionalQuotationLineIDRequestBuilder.OptionalQuotationLineIDRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.CRMOptionalQuotationLineIDResults is null)
        {
            return response;
        }

        var allItems = new List<CRMOptionalQuotationLineID>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.CRMOptionalQuotationLineIDResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.CRMOptionalQuotationLineIDResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new OptionalQuotationLineIDRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new CRMOptionalQuotationLineID_Response
        {
            D = new CRMOptionalQuotationLineID_Response.CRMOptionalQuotationLineID_Response_d
            {
                CRMOptionalQuotationLineID = allItems
            }
        };

        return finalResponse;
    }
}
