// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Mailbox.MailMessagesSent;

public partial class MailMessagesSentRequestBuilder
{
    public async Task<MailboxMailMessagesSent_Response?> GetAllAsync(Action<RequestConfiguration<MailMessagesSentRequestBuilder.MailMessagesSentRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.MailboxMailMessagesSentResults is null)
        {
            return response;
        }

        var allItems = new List<MailboxMailMessagesSent>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.MailboxMailMessagesSentResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.MailboxMailMessagesSentResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new MailMessagesSentRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new MailboxMailMessagesSent_Response
        {
            D = new MailboxMailMessagesSent_Response.MailboxMailMessagesSent_Response_d
            {
                MailboxMailMessagesSent = allItems
            }
        };

        return finalResponse;
    }
}
