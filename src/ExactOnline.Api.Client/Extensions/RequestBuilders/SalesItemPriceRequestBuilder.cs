// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Logistics.SalesItemPrice;

public partial class SalesItemPriceRequestBuilder
{
    public async Task<ReadLogisticsSalesItemPrice_Response?> GetAllAsync(Action<RequestConfiguration<SalesItemPriceRequestBuilder.SalesItemPriceRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadLogisticsSalesItemPriceResults is null)
        {
            return response;
        }

        var allItems = new List<ReadLogisticsSalesItemPrice>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadLogisticsSalesItemPriceResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadLogisticsSalesItemPriceResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new SalesItemPriceRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadLogisticsSalesItemPrice_Response
        {
            D = new ReadLogisticsSalesItemPrice_Response.ReadLogisticsSalesItemPrice_Response_d
            {
                ReadLogisticsSalesItemPrice = allItems
            }
        };

        return finalResponse;
    }
}
