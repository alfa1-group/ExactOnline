// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Crm.RejectQuotation;

public partial class RejectQuotationRequestBuilder
{
    public async Task<CRMRejectQuotation_Response?> GetAllAsync(Action<RequestConfiguration<RejectQuotationRequestBuilder.RejectQuotationRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.CRMRejectQuotationResults is null)
        {
            return response;
        }

        var allItems = new List<CRMRejectQuotation>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.CRMRejectQuotationResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.CRMRejectQuotationResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new RejectQuotationRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new CRMRejectQuotation_Response
        {
            D = new CRMRejectQuotation_Response.CRMRejectQuotation_Response_d
            {
                CRMRejectQuotation = allItems
            }
        };

        return finalResponse;
    }
}
