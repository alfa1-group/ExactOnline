// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Crm.ReviewQuotation;

public partial class ReviewQuotationRequestBuilder
{
    public async Task<CRMReviewQuotation_Response?> GetAllAsync(Action<RequestConfiguration<ReviewQuotationRequestBuilder.ReviewQuotationRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.CRMReviewQuotationResults is null)
        {
            return response;
        }

        var allItems = new List<CRMReviewQuotation>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.CRMReviewQuotationResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.CRMReviewQuotationResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ReviewQuotationRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new CRMReviewQuotation_Response
        {
            D = new CRMReviewQuotation_Response.CRMReviewQuotation_Response_d
            {
                CRMReviewQuotation = allItems
            }
        };

        return finalResponse;
    }
}
