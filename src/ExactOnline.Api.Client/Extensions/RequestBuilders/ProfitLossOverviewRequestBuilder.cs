// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Financial.ProfitLossOverview;

public partial class ProfitLossOverviewRequestBuilder
{
    public async Task<ReadFinancialProfitLossOverview_Response?> GetAllAsync(Action<RequestConfiguration<ProfitLossOverviewRequestBuilder.ProfitLossOverviewRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadFinancialProfitLossOverviewResults is null)
        {
            return response;
        }

        var allItems = new List<ReadFinancialProfitLossOverview>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadFinancialProfitLossOverviewResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadFinancialProfitLossOverviewResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ProfitLossOverviewRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadFinancialProfitLossOverview_Response
        {
            D = new ReadFinancialProfitLossOverview_Response.ReadFinancialProfitLossOverview_Response_d
            {
                ReadFinancialProfitLossOverview = allItems
            }
        };

        return finalResponse;
    }
}
