// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Salesinvoice.SalesOrderID;

public partial class SalesOrderIDRequestBuilder
{
    public async Task<SalesInvoiceSalesOrderID_Response?> GetAllAsync(Action<RequestConfiguration<SalesOrderIDRequestBuilder.SalesOrderIDRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.SalesInvoiceSalesOrderIDResults is null)
        {
            return response;
        }

        var allItems = new List<SalesInvoiceSalesOrderID>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.SalesInvoiceSalesOrderIDResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.SalesInvoiceSalesOrderIDResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new SalesOrderIDRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new SalesInvoiceSalesOrderID_Response
        {
            D = new SalesInvoiceSalesOrderID_Response.SalesInvoiceSalesOrderID_Response_d
            {
                SalesInvoiceSalesOrderID = allItems
            }
        };

        return finalResponse;
    }
}
