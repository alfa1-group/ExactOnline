// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Current.Me;

public partial class MeRequestBuilder
{
    public async Task<SystemSystemMe_Response?> GetAllAsync(Action<RequestConfiguration<MeRequestBuilder.MeRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.SystemSystemMeResults is null)
        {
            return response;
        }

        var allItems = new List<SystemSystemMe>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.SystemSystemMeResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.SystemSystemMeResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new MeRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new SystemSystemMe_Response
        {
            D = new SystemSystemMe_Response.SystemSystemMe_Response_d
            {
                SystemSystemMe = allItems
            }
        };

        return finalResponse;
    }
}
