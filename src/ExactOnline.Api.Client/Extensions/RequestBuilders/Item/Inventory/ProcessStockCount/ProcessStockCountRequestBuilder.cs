// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Inventory.ProcessStockCount;

public partial class ProcessStockCountRequestBuilder
{
    public async Task<InventoryProcessStockCount_Response?> GetAllAsync(Action<RequestConfiguration<ProcessStockCountRequestBuilder.ProcessStockCountRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.InventoryProcessStockCountResults is null)
        {
            return response;
        }

        var allItems = new List<InventoryProcessStockCount>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.InventoryProcessStockCountResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.InventoryProcessStockCountResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ProcessStockCountRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new InventoryProcessStockCount_Response
        {
            D = new InventoryProcessStockCount_Response.InventoryProcessStockCount_Response_d
            {
                InventoryProcessStockCount = allItems
            }
        };

        return finalResponse;
    }
}
