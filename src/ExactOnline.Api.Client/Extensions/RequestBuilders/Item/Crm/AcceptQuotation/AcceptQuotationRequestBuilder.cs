// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Crm.AcceptQuotation;

public partial class AcceptQuotationRequestBuilder
{
    public async Task<CRMAcceptQuotation_Response?> GetAllAsync(Action<RequestConfiguration<AcceptQuotationRequestBuilder.AcceptQuotationRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.CRMAcceptQuotationResults is null)
        {
            return response;
        }

        var allItems = new List<CRMAcceptQuotation>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.CRMAcceptQuotationResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.CRMAcceptQuotationResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new AcceptQuotationRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new CRMAcceptQuotation_Response
        {
            D = new CRMAcceptQuotation_Response.CRMAcceptQuotation_Response_d
            {
                CRMAcceptQuotation = allItems
            }
        };

        return finalResponse;
    }
}
