// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Crm.PrintQuotation;

public partial class PrintQuotationRequestBuilder
{
    public async Task<CRMPrintQuotation_Response?> GetAllAsync(Action<RequestConfiguration<PrintQuotationRequestBuilder.PrintQuotationRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.CRMPrintQuotationResults is null)
        {
            return response;
        }

        var allItems = new List<CRMPrintQuotation>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.CRMPrintQuotationResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.CRMPrintQuotationResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new PrintQuotationRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new CRMPrintQuotation_Response
        {
            D = new CRMPrintQuotation_Response.CRMPrintQuotation_Response_d
            {
                CRMPrintQuotation = allItems
            }
        };

        return finalResponse;
    }
}
