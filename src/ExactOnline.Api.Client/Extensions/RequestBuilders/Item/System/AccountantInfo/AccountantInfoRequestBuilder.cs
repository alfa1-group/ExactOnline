// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.System.AccountantInfo;

public partial class AccountantInfoRequestBuilder
{
    public async Task<SystemSystemAccountantInfo_Response?> GetAllAsync(Action<RequestConfiguration<AccountantInfoRequestBuilder.AccountantInfoRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.SystemSystemAccountantInfoResults is null)
        {
            return response;
        }

        var allItems = new List<SystemSystemAccountantInfo>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.SystemSystemAccountantInfoResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.SystemSystemAccountantInfoResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new AccountantInfoRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new SystemSystemAccountantInfo_Response
        {
            D = new SystemSystemAccountantInfo_Response.SystemSystemAccountantInfo_Response_d
            {
                SystemSystemAccountantInfo = allItems
            }
        };

        return finalResponse;
    }
}
