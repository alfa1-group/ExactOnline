// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Openingbalance.CurrentYear.Processed;

public partial class ProcessedRequestBuilder
{
    public async Task<OpeningBalanceCurrentYearProcessed_Response?> GetAllAsync(Action<RequestConfiguration<ProcessedRequestBuilder.ProcessedRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.OpeningBalanceCurrentYearProcessedResults is null)
        {
            return response;
        }

        var allItems = new List<OpeningBalanceCurrentYearProcessed>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.OpeningBalanceCurrentYearProcessedResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.OpeningBalanceCurrentYearProcessedResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ProcessedRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new OpeningBalanceCurrentYearProcessed_Response
        {
            D = new OpeningBalanceCurrentYearProcessed_Response.OpeningBalanceCurrentYearProcessed_Response_d
            {
                OpeningBalanceCurrentYearProcessed = allItems
            }
        };

        return finalResponse;
    }
}
