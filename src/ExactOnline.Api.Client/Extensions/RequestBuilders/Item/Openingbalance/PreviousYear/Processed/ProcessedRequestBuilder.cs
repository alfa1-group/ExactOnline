// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Openingbalance.PreviousYear.Processed;

public partial class ProcessedRequestBuilder
{
    public async Task<OpeningBalancePreviousYearProcessed_Response?> GetAllAsync(Action<RequestConfiguration<ProcessedRequestBuilder.ProcessedRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.OpeningBalancePreviousYearProcessedResults is null)
        {
            return response;
        }

        var allItems = new List<OpeningBalancePreviousYearProcessed>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.OpeningBalancePreviousYearProcessedResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.OpeningBalancePreviousYearProcessedResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ProcessedRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new OpeningBalancePreviousYearProcessed_Response
        {
            D = new OpeningBalancePreviousYearProcessed_Response.OpeningBalancePreviousYearProcessed_Response_d
            {
                OpeningBalancePreviousYearProcessed = allItems
            }
        };

        return finalResponse;
    }
}
