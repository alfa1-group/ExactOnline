// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Logistics.ItemAssortment;

public partial class ItemAssortmentRequestBuilder
{
    public async Task<LogisticsItemAssortment_Response?> GetAllAsync(Action<RequestConfiguration<ItemAssortmentRequestBuilder.ItemAssortmentRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.LogisticsItemAssortmentResults is null)
        {
            return response;
        }

        var allItems = new List<LogisticsItemAssortment>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.LogisticsItemAssortmentResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.LogisticsItemAssortmentResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ItemAssortmentRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new LogisticsItemAssortment_Response
        {
            D = new LogisticsItemAssortment_Response.LogisticsItemAssortment_Response_d
            {
                LogisticsItemAssortment = allItems
            }
        };

        return finalResponse;
    }
}
