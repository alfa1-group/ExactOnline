// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Project.ProjectPlanning;

public partial class ProjectPlanningRequestBuilder
{
    public async Task<ProjectProjectPlanning_Response?> GetAllAsync(Action<RequestConfiguration<ProjectPlanningRequestBuilder.ProjectPlanningRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ProjectProjectPlanningResults is null)
        {
            return response;
        }

        var allItems = new List<ProjectProjectPlanning>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ProjectProjectPlanningResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ProjectProjectPlanningResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ProjectPlanningRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ProjectProjectPlanning_Response
        {
            D = new ProjectProjectPlanning_Response.ProjectProjectPlanning_Response_d
            {
                ProjectProjectPlanning = allItems
            }
        };

        return finalResponse;
    }
}
