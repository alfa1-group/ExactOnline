// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Project.CostsByDate;

public partial class CostsByDateRequestBuilder
{
    public async Task<ReadProjectCostsByDate_Response?> GetAllAsync(Action<RequestConfiguration<CostsByDateRequestBuilder.CostsByDateRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadProjectCostsByDateResults is null)
        {
            return response;
        }

        var allItems = new List<ReadProjectCostsByDate>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadProjectCostsByDateResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadProjectCostsByDateResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new CostsByDateRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadProjectCostsByDate_Response
        {
            D = new ReadProjectCostsByDate_Response.ReadProjectCostsByDate_Response_d
            {
                ReadProjectCostsByDate = allItems
            }
        };

        return finalResponse;
    }
}
