// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Project.HoursById;

public partial class HoursByIdRequestBuilder
{
    public async Task<ReadProjectHoursById_Response?> GetAllAsync(Action<RequestConfiguration<HoursByIdRequestBuilder.HoursByIdRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadProjectHoursByIdResults is null)
        {
            return response;
        }

        var allItems = new List<ReadProjectHoursById>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadProjectHoursByIdResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadProjectHoursByIdResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new HoursByIdRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadProjectHoursById_Response
        {
            D = new ReadProjectHoursById_Response.ReadProjectHoursById_Response_d
            {
                ReadProjectHoursById = allItems
            }
        };

        return finalResponse;
    }
}
