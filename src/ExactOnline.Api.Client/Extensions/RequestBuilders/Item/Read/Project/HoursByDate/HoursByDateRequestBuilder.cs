// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Project.HoursByDate;

public partial class HoursByDateRequestBuilder
{
    public async Task<ReadProjectHoursByDate_Response?> GetAllAsync(Action<RequestConfiguration<HoursByDateRequestBuilder.HoursByDateRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadProjectHoursByDateResults is null)
        {
            return response;
        }

        var allItems = new List<ReadProjectHoursByDate>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadProjectHoursByDateResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadProjectHoursByDateResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new HoursByDateRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadProjectHoursByDate_Response
        {
            D = new ReadProjectHoursByDate_Response.ReadProjectHoursByDate_Response_d
            {
                ReadProjectHoursByDate = allItems
            }
        };

        return finalResponse;
    }
}
