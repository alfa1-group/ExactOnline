// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Financial.JournalStatusList;

public partial class JournalStatusListRequestBuilder
{
    public async Task<ReadFinancialJournalStatusList_Response?> GetAllAsync(Action<RequestConfiguration<JournalStatusListRequestBuilder.JournalStatusListRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadFinancialJournalStatusListResults is null)
        {
            return response;
        }

        var allItems = new List<ReadFinancialJournalStatusList>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadFinancialJournalStatusListResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadFinancialJournalStatusListResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new JournalStatusListRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadFinancialJournalStatusList_Response
        {
            D = new ReadFinancialJournalStatusList_Response.ReadFinancialJournalStatusList_Response_d
            {
                ReadFinancialJournalStatusList = allItems
            }
        };

        return finalResponse;
    }
}
