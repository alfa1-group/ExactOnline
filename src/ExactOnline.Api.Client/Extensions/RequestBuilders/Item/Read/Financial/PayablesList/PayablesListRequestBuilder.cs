// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Financial.PayablesList;

public partial class PayablesListRequestBuilder
{
    public async Task<ReadFinancialPayablesList_Response?> GetAllAsync(Action<RequestConfiguration<PayablesListRequestBuilder.PayablesListRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadFinancialPayablesListResults is null)
        {
            return response;
        }

        var allItems = new List<ReadFinancialPayablesList>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadFinancialPayablesListResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadFinancialPayablesListResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new PayablesListRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadFinancialPayablesList_Response
        {
            D = new ReadFinancialPayablesList_Response.ReadFinancialPayablesList_Response_d
            {
                ReadFinancialPayablesList = allItems
            }
        };

        return finalResponse;
    }
}
