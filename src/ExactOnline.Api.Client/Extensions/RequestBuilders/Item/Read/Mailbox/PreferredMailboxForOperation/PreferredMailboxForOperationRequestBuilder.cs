// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Mailbox.PreferredMailboxForOperation;

public partial class PreferredMailboxForOperationRequestBuilder
{
    public async Task<ReadMailboxPreferredMailboxForOperation_Response?> GetAllAsync(Action<RequestConfiguration<PreferredMailboxForOperationRequestBuilder.PreferredMailboxForOperationRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadMailboxPreferredMailboxForOperationResults is null)
        {
            return response;
        }

        var allItems = new List<ReadMailboxPreferredMailboxForOperation>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadMailboxPreferredMailboxForOperationResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadMailboxPreferredMailboxForOperationResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new PreferredMailboxForOperationRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadMailboxPreferredMailboxForOperation_Response
        {
            D = new ReadMailboxPreferredMailboxForOperation_Response.ReadMailboxPreferredMailboxForOperation_Response_d
            {
                ReadMailboxPreferredMailboxForOperation = allItems
            }
        };

        return finalResponse;
    }
}
