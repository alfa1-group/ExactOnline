// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Mailbox.DefaultMailbox;

public partial class DefaultMailboxRequestBuilder
{
    public async Task<ReadMailboxDefaultMailbox_Response?> GetAllAsync(Action<RequestConfiguration<DefaultMailboxRequestBuilder.DefaultMailboxRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadMailboxDefaultMailboxResults is null)
        {
            return response;
        }

        var allItems = new List<ReadMailboxDefaultMailbox>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadMailboxDefaultMailboxResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadMailboxDefaultMailboxResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new DefaultMailboxRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadMailboxDefaultMailbox_Response
        {
            D = new ReadMailboxDefaultMailbox_Response.ReadMailboxDefaultMailbox_Response_d
            {
                ReadMailboxDefaultMailbox = allItems
            }
        };

        return finalResponse;
    }
}
