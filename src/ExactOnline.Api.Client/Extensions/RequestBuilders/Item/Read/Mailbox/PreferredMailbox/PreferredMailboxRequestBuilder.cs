// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Read.Mailbox.PreferredMailbox;

public partial class PreferredMailboxRequestBuilder
{
    public async Task<ReadMailboxPreferredMailbox_Response?> GetAllAsync(Action<RequestConfiguration<PreferredMailboxRequestBuilder.PreferredMailboxRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ReadMailboxPreferredMailboxResults is null)
        {
            return response;
        }

        var allItems = new List<ReadMailboxPreferredMailbox>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ReadMailboxPreferredMailboxResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ReadMailboxPreferredMailboxResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new PreferredMailboxRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ReadMailboxPreferredMailbox_Response
        {
            D = new ReadMailboxPreferredMailbox_Response.ReadMailboxPreferredMailbox_Response_d
            {
                ReadMailboxPreferredMailbox = allItems
            }
        };

        return finalResponse;
    }
}
