// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Expensemanagement.Expenses;

public partial class ExpensesRequestBuilder
{
    public async Task<ExpenseManagementExpense_Response?> GetAllAsync(Action<RequestConfiguration<ExpensesRequestBuilder.ExpensesRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ExpenseManagementExpenseResults is null)
        {
            return response;
        }

        var allItems = new List<ExpenseManagementExpense>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ExpenseManagementExpenseResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ExpenseManagementExpenseResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new ExpensesRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ExpenseManagementExpense_Response
        {
            D = new ExpenseManagementExpense_Response.ExpenseManagementExpense_Response_d
            {
                ExpenseManagementExpense = allItems
            }
        };

        return finalResponse;
    }
}
