// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Activities.CommunicationNotes;

public partial class CommunicationNotesRequestBuilder
{
    public async Task<ActivitiesCommunicationNote_Response?> GetAllAsync(Action<RequestConfiguration<CommunicationNotesRequestBuilder.CommunicationNotesRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ActivitiesCommunicationNoteResults is null)
        {
            return response;
        }

        var allItems = new List<ActivitiesCommunicationNote>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ActivitiesCommunicationNoteResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ActivitiesCommunicationNoteResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new CommunicationNotesRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ActivitiesCommunicationNote_Response
        {
            D = new ActivitiesCommunicationNote_Response.ActivitiesCommunicationNote_Response_d
            {
                ActivitiesCommunicationNote = allItems
            }
        };

        return finalResponse;
    }
}
