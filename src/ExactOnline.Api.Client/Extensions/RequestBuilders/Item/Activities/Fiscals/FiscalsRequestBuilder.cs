// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Activities.Fiscals;

public partial class FiscalsRequestBuilder
{
    public async Task<ActivitiesFiscal_Response?> GetAllAsync(Action<RequestConfiguration<FiscalsRequestBuilder.FiscalsRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.ActivitiesFiscalResults is null)
        {
            return response;
        }

        var allItems = new List<ActivitiesFiscal>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.ActivitiesFiscalResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.ActivitiesFiscalResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new FiscalsRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new ActivitiesFiscal_Response
        {
            D = new ActivitiesFiscal_Response.ActivitiesFiscal_Response_d
            {
                ActivitiesFiscal = allItems
            }
        };

        return finalResponse;
    }
}
