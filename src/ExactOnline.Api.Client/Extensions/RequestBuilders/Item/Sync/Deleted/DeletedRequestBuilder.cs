// <auto-generated/>
#nullable enable
using System.Linq.Expressions;
using ExactOnline.Api.Client.Models;
using Microsoft.Kiota.Abstractions;
using Microsoft.Kiota.Abstractions.Serialization;

namespace ExactOnline.Api.Client.Api.V1.Item.Sync.Deleted;

public partial class DeletedRequestBuilder
{
    public async Task<SyncDeleted_Response?> GetAllAsync(Action<RequestConfiguration<DeletedRequestBuilder.DeletedRequestBuilderGetQueryParameters>>? requestConfiguration = default, CancellationToken cancellationToken = default)
    {
        var response = await GetAsync(requestConfiguration, cancellationToken);
        if (response?.D?.SyncDeletedResults is null)
        {
            return response;
        }

        var allItems = new List<SyncDeleted>();
        var currentResponse = response;

        while (true)
        {
            if (currentResponse?.D?.SyncDeletedResults?.Results is { } results)
            {
                allItems.AddRange(results);
            }

            var nextUrl = currentResponse?.D?.SyncDeletedResults?.Next;
            if (string.IsNullOrEmpty(nextUrl))
            {
                break;
            }

            var nextRequestBuilder = new DeletedRequestBuilder(nextUrl, RequestAdapter);
            currentResponse = await nextRequestBuilder.GetAsync(cancellationToken: cancellationToken);
        }

        var finalResponse = new SyncDeleted_Response
        {
            D = new SyncDeleted_Response.SyncDeleted_Response_d
            {
                SyncDeleted = allItems
            }
        };

        return finalResponse;
    }
}
